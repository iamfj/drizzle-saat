{
  "name": "drizzle-saat-example-analytics-events",
  "private": true,
  "type": "module",
  "scripts": {
    "gen": "drizzle-saat generate",
    "typecheck": "drizzle-saat generate && tsc --noEmit",
    "seed": "bun seed.ts",
    "scenarios": "bun seed.ts --scenarios",
    "verify": "bun seed.ts --verify",
    "reset": "rm -rf dev.db .drizzle-saat",
    "postinstall": "drizzle-saat generate"
  },
  "dependencies": {
    "drizzle-saat": "file:../..",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
