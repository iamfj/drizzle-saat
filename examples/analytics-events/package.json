{
  "name": "saat-example-analytics-events",
  "private": true,
  "type": "module",
  "scripts": {
    "gen": "saat generate",
    "typecheck": "saat generate && tsc --noEmit",
    "seed": "bun seed.ts",
    "scenarios": "bun seed.ts --scenarios",
    "verify": "bun seed.ts --verify",
    "reset": "rm -rf dev.db .saat",
    "postinstall": "saat generate"
  },
  "dependencies": {
    "saat": "file:../..",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
