{
  "name": "saat-example-testing-with-saat",
  "private": true,
  "type": "module",
  "scripts": {
    "gen": "saat generate",
    "typecheck": "saat generate && tsc --noEmit",
    "test": "bun test",
    "seed": "bun seed.ts",
    "verify": "bun seed.ts --verify",
    "reset": "rm -rf dev.db .saat",
    "postinstall": "saat generate"
  },
  "dependencies": {
    "saat": "file:../..",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
